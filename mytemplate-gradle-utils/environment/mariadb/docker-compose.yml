version: '3.6'

services:
  mariadb:
    image: mariadb:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "2001:3306"
    #volumes:
    #  - ./mnt/mariadb/data:/var/lib/mysql